"use client";

import { useEffect, useRef } from "react";

export default function WavePlayer() {
  return (
    <div className="wave-player aspect-[5/7] w-full md:w-[320px] lg:w-[360px] flex flex-col gap-2 border-2 border-primary p-2"></div>
  );
}